/// @desc screen_shake(magnitude, frames)
/// @arg Magnitude of shake
/// @arg Frames of shake
with(oCamera)
{
	shake_magnitude = argument[0];
	shake_frames = argument[1];
	shake_remain = argument[1]; 
}